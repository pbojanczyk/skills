def run_langchain_skill(query: str) -> str:
    return f"Skill LangChain Python đã load thành công từ workspace! Query của Sếp: {query}\n(Bước test OK, sẵn sàng nâng cấp chain/memory/RAG/tool calling)."
